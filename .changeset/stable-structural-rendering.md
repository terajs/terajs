---
"@terajs/reactivity": minor
"@terajs/renderer-web": minor
"@terajs/app": minor
---

Export the public `batch()` API, preserve mounted `v-if` branches while their selected branch is unchanged, and reuse compiler-generated keyed `v-for` rows with live plain loop locals.
