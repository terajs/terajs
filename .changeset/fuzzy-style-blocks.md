---
"@terajs/vite-plugin": patch
"@terajs/app": patch
---

Compile `.tera` style blocks in the Vite plugin and register them through the public app runtime surface, including scoped CSS output and HMR cleanup.
