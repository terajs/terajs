---
"@terajs/runtime": minor
"@terajs/app": minor
---

Add adaptive local-first storage profiles, metadata-aware persistence adapters, memory/IndexedDB/forbidden persistence adapters, and bucket primitives for app-owned large payload storage. Resources can now use app-selected persistence adapters instead of being limited to localStorage.
