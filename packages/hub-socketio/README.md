# @terajs/hub-socketio

Socket.IO transport adapter for Terajs sync hub workflows.

This package implements the runtime `ServerFunctionTransport` contract and emits `hub:*` debug events for devtools visibility.